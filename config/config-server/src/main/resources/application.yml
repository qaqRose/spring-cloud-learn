spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          username:
          password:
          uri: https://github.com/forezp/SpringcloudConfig/
        searchPaths: respo
      label: master

server:
  port: 8888






